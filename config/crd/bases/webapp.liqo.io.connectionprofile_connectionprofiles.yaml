
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: connectionprofiles.webapp.liqo.io.connectionprofile
spec:
  group: webapp.liqo.io.connectionprofile
  names:
    kind: ConnectionProfile
    listKind: ConnectionProfileList
    plural: connectionprofiles
    singular: connectionprofile
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: ConnectionProfile is the Schema for the connectionprofiles API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ConnectionProfileSpec defines the desired state of ConnectionProfile
          properties:
            bandwidth_requirement:
              type: string
            destination_job:
              type: string
            source_job:
              description: Foo is an example field of ConnectionProfile. Edit ConnectionProfile_types.go
                to remove/update
              type: string
            timeslot:
              type: string
            update_time:
              type: string
          required:
          - bandwidth_requirement
          - destination_job
          - source_job
          - timeslot
          - update_time
          type: object
        status:
          description: ConnectionProfileStatus defines the observed state of ConnectionProfile
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
